@page
@model cs392_demo.Pages.Users_Page.EditModel

@{
    ViewData["Title"] = "Edit";
}

<section class="edit-shell">
    <header class="edit-header">
        <h1>Edit User</h1>
        <p class="inventory-subtitle">Update account details and save your changes.</p>
    </header>

    <div class="edit-card">
        <form method="post" class="edit-form">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Users.UserName" class="control-label"></label>
                <input asp-for="Users.UserName" class="form-control" />
                <span asp-validation-for="Users.UserName" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="Users.Id" />
            <div class="form-group">
                <label asp-for="Users.Email" class="control-label"></label>
                <input asp-for="Users.Email" class="form-control" />
                <span asp-validation-for="Users.Email" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Users.Role_Database" class="control-label"></label>
                <input asp-for="Users.Role_Database" class="form-control" />
                <span asp-validation-for="Users.Role_Database" class="text-danger"></span>
            </div>
            <div class="edit-actions">
                <button type="submit" class="btn btn-primary">Save</button>
                <a class="btn btn-outline-secondary" asp-page="./Index">Back to List</a>
            </div>
        </form>
    </div>
</section>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
