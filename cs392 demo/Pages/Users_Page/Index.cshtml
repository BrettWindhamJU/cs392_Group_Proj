@page
@model cs392_demo.Pages.Users_Page.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Users[0].Username)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Users[0].Password_account)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Users[0].Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Users[0].Role_Database)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Users) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Username)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Password_account)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Role_Database)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.User_ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.User_ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.User_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
